name: legal_reader
description: تطبيق قراءة PDF و Word للمستندات القانونية
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^10.3.10
  doc_text_extractor: ^2.1.0
  flutter_tts: ^4.2.5
  shared_preferences: ^2.3.0
  path_provider: ^2.1.4
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
